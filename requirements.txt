# I upgraded to the modern LangChain 1.x stack + create_agent (built on
# LangGraph) specifically because Gemini's newer "thinking" models (3.x)
# require a "thought_signature" to be echoed back on every tool call.
# The old AgentExecutor architecture (LangChain 0.3.x) dropped this
# signature when rebuilding its scratchpad, causing a 400 error from
# the Gemini API. create_agent keeps the original message objects
# intact across turns, so the signature survives correctly.
langchain
langchain-openai
langchain-community
langchain-core
langchain-google-genai==4.4.0
python-dotenv
pandas
SQLAlchemy
pydantic
tavily-python
